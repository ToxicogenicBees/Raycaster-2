/*
    TextureColorSpace.hpp

    Declaration of an enum for different texture color spaces.
*/

#pragma once

namespace toxico {
    enum class TextureColorSpace {
        Linear,
        sRGB,
    };
}
