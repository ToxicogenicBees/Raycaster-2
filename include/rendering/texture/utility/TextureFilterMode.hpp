/*
    TextureFilterMode.hpp

    Declaration of an enum for different texture filter modes.
*/

#pragma once

namespace toxico {
    enum class TextureFilterMode {
        Nearest,
        Bilinear,
    };
}
